import React from "react";

const MainSection = () => {
  return (
    <section className="MainSection flexi h-56 w-full bg-[radial-gradient(ellipse_at_top,_var(--tw-gradient-stops))] from-primary-400 to-primary-600">
      MainSection
    </section>
  );
};

export default MainSection;
