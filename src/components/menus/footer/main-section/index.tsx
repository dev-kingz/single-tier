import React from "react";

const MainSection = () => {
  return (
    <section className="MainSection from-primary-400 to-primary-600 flexi h-56 w-full bg-[radial-gradient(ellipse_at_top,_var(--tw-gradient-stops))]">
      MainSection
    </section>
  );
};

export default MainSection;
